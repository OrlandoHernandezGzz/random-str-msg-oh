#!/usr/bin/env node
let random = require('../src/index.js');

random.funnyCommit();

/**
 * Genera una instrucción para indicarle que el archivo
 * va a funcionar del lado de node, así como hacer un import
 * del modulo que acabamos de crear y luego ejecuta la acción
 */